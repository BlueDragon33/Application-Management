name: Application Management Local Offline Smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Heavy multi-repository offline smoke is manual during Development Mode."
